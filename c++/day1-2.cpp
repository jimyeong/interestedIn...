#include <iostream>
using namespace std;

int main(int argc, char const *argv[]) {
    // compile 타임시
    int num_students;
    const int num_student = 4;

    // runtime  시 결정
    cin >> num_students;

    // 에러가 나는 안나네... 컴파일 시 에 length 가 결정되야 한다고 했는데.
    int student_scores[num_student];

    /* code */
    return 0;
}
